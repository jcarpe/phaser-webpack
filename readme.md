# Phaser Webpack Build

Commands:
```
npm install -g npx
npm install
npm run dev
```

### Note:

This project was not directly forked, but _heavily_ informed by https://github.com/lean/phaser-es6-webpack
